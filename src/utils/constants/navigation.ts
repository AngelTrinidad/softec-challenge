import { StackNavigationOptions } from '@react-navigation/stack';

export const DEFAULT_NAVIGATION_OPTIONS: StackNavigationOptions = {
  headerShown: false,
  cardOverlayEnabled: true,
  headerMode: 'screen',
};
