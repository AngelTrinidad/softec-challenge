import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    width: '100%',
    minHeight: 50,
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 32,
    paddingVertical: 20,
  },
  title: {
    color: 'white',
    fontWeight: '600',
  },
});
