import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  centerContainer: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  errorText: {
    marginBottom: 24,
  },
});
